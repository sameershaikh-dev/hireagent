# HireAgent Environment Variables
# The API key is entered via the UI — no .env needed for basic use.
# Optionally you can pre-set the key here and remove the API key field from the UI.

# GEMINI_API_KEY=your_key_here